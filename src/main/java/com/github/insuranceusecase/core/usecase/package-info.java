/**
 * At this level, we have, what Alistair calls: different "summary goals". Summary
 * goals are collection of related "user goals" (use cases proper). In our example,
 * we may regroup all use cases related to insurance claims processing into a
 * special package which will be different from the package containing user goals
 * for registration of clients.
 */
package com.github.insuranceusecase.core.usecase;